# Outbound network. ONLY the cloudflared container joins this one, so the
# tunnel can reach the Cloudflare edge while nginx stays sealed off.
[Unit]
Description=WebSWR egress network (tunnel only)

[Network]
NetworkName=webswr-egress
